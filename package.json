{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "MERN Portfolio with Admin Panel",
  "scripts": {
    "install:all": "cd client && npm install && cd ../server && npm install",
    "dev": "echo 'Run client: cd client && npm run dev' && echo 'Run server: cd server && npm run dev'",
    "build": "cd client && npm run build",
    "start": "cd client && npm start"
  }
}
